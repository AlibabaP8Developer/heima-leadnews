server:
  port: 51801
spring:
  application:
    name: leadnews-user
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: localhost:8848
      discovery:
        namespace: dev
        group: leadnews-user-group
      config:
        namespace: dev
        group: leadnews-user-group
        file-extension: yaml
        refresh-enabled: true
